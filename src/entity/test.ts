export enum testEntityVar {
    mongo = 'maximum',
    typo = 'royal',
    vivo = 'next',
    max = 'tool',
    goal = 'goal',
}

export enum mariaVar {
    yes = 'not out',
    no = 'out',
}

export enum mamariaVar {
    yes = 'not out',
    no = 'out',
}

export enum coco {
    yes = 'not out',
    no = 'out',
}

export enum popy {
    yes = 'not out',
    no = 'out',
}
