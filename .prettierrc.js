module.exports = {
    // Specify the line length that the printer will wrap on.
    printWidth: 300,
    // Specify the number of spaces per indentation-level.
    tabWidth: 4,
    // Use tabs instead of spaces.
    useTabs: false,
    // Use single quotes instead of double quotes.
    singleQuote: true,
    // Change when properties in objects are quoted.
    quoteProps: 'as-needed',
    // Use single quotes instead of double quotes in JSX.
    jsxSingleQuote: false,
    // Print semicolons at the ends of statements.
    semi: true,
    // Include parentheses around a sole arrow function parameter.
    arrowParens: 'always',
    // Use spaces inside braces of object literals.
    bracketSpacing: true,
    // Put the `>` of a multi-line JSX element at the end of the last line instead of being alone on the next line.
    jsxBracketSameLine: false,
    // Specify the global strict mode directive.
    requirePragma: false,
    // Insert a new line at the end of files.
    endOfLine: 'auto',
    // Control the printing of trailing commas wherever possible.
    // Valid options: "none", "es5", "all"
    trailingComma: 'all',
    // Print spaces between brackets in object literals.
    bracketSpacing: true,
    // Include parentheses around a sole arrow function parameter.
    arrowParens: 'always',
};
